apiVersion: v1
kind: Service
metadata:
  name: {{ include "shift-roster.fullname" . }}
  labels:
    {{- include "shift-roster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shift-roster.selectorLabels" . | nindent 4 }}
